
server:
  port: 8761
spring:
  application:
    name: spring-eureka
  security:
    user:
      name: admin
      password: niaoren
eureka:
  instance:
    hostname: localhost
  client:
    fetch-registry: false
    register-with-eureka: false
    service-url:
      defaultZone: http://admin:niaoren@${eureka.instance.hostname}:${server.port}/eureka/
